name: niq-application

services:
  postgres:
    image: postgres:17.6-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: niq_application
      POSTGRES_USER: niq
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me-locally}
    volumes:
      - application-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U niq -d niq_application"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks: [private]

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      APP_REGION: ${APP_REGION:-india}
      DEPLOYMENT_MODE: ${DEPLOYMENT_MODE:-on-prem}
      API_HOST: 0.0.0.0
      API_PORT: 3000
      WEB_ORIGIN: ${WEB_ORIGIN:-http://localhost:8080}
      DATABASE_URL: postgres://niq:${POSTGRES_PASSWORD:-change-me-locally}@postgres:5432/niq_application
      DATABASE_SSL: "false"
      AUTH_MODE: ${AUTH_MODE:-disabled}
      SESSION_SECRET: ${SESSION_SECRET:-replace-this-development-only-secret}
      PATIENT_DATA_ENCRYPTION_KEY: ${PATIENT_DATA_ENCRYPTION_KEY:-}
      PATIENT_DATA_KEY_VERSION: ${PATIENT_DATA_KEY_VERSION:-local-v1}
      MFA_RESEND_COOLDOWN_SECONDS: ${MFA_RESEND_COOLDOWN_SECONDS:-30}
      MFA_MAX_RESENDS: ${MFA_MAX_RESENDS:-3}
      SCORING_API_URL: ${SCORING_API_URL:-}
      SCORING_TIMEOUT_MS: ${SCORING_TIMEOUT_MS:-10000}
      SCORING_CREDENTIAL_ENCRYPTION_KEY: ${SCORING_CREDENTIAL_ENCRYPTION_KEY:-}
      SCORING_CREDENTIAL_KEY_VERSION: ${SCORING_CREDENTIAL_KEY_VERSION:-local-v1}
      FACE_SCAN_ENABLED: ${FACE_SCAN_ENABLED:-false}
      FACE_SCAN_RECONCILE_INTERVAL_MS: ${FACE_SCAN_RECONCILE_INTERVAL_MS:-30000}
      # Pass through only when supplied; an empty token is invalid.
      BOOTSTRAP_TOKEN:
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    ports: ["3000:3000"]
    networks: [private, edge]

  migrate:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: migrations
    environment:
      DATABASE_URL: postgres://niq:${POSTGRES_PASSWORD:-change-me-locally}@postgres:5432/niq_application
      DATABASE_SSL: "false"
    depends_on:
      postgres:
        condition: service_healthy
    networks: [private]

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    depends_on: [api]
    ports: ["8080:8080"]
    networks: [edge]

networks:
  private:
    internal: true
  edge:

volumes:
  application-postgres:
